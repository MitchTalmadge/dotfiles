(test $0 = "tcsh" || test $0 = "csh") || eval 'setenv() { export "$1=$2"; }'

echo "> Executing all .env_vars"
setenv DOTFILES_VERSION "`cat ~/.dotfiles/bin/migrations/current_version`"
setenv PATH "$PATH:~/bin/"
