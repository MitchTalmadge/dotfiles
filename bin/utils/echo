# Only echo when interactive
alias echoi='true' 
# Disabled below for now... causing conflicts with tools that don't expect output
# case "$-" in  
# *i* ) alias echoi='echo' ;;
# *   ) alias echoi='true' ;;
# esac
